{
   server-conf {
        host = "localhost"
        port = 7069
   }

   customer-details-service-config {
        host = "localhost"
        port = 8080
   }

   customer-details-legacy-config {
       host = "localhost"
       port = 8081
   }

}